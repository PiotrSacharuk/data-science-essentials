[pytest]

# Ensure the repository root is in PYTHONPATH
pythonpath = .

testpaths = tests
python_files = test_*.py

# Coverage configuration
addopts = --cov=src --cov=app --cov-report=term-missing --cov-report=html
